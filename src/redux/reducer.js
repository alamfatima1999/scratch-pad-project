import actionTypes from './actionTypes';


// Adding Initial State here
const INITIAL_STATE = {
  
};

// Reducer added for scratch Pad
const reducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default reducer;